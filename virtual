 VIRTUAL adj. 1. Common alternative to LOGICAL (q.v.), but never used with
compass directions. 2. Performing the functions of. Virtual memory acts like
real memory but isn't. This word is nearly synonymous with LOGICAL, but is
never used of directions. Note that for any thing X, a logical X is either a
real X or a virtual X, but not both.