 BOOT (boot) [from "by one's bootstraps"] v.,n. To load and initialize the
operating system on a machine. This usage is no longer slang (having become
jargon in the strict sense), but the following variants still are. COLD BOOT:
a boot from a power- off condition. WARM BOOT: a reboot with the CPU and all
devices already powered up, as after a hardware reset or software crash.